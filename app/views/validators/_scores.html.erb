<a class="small text-nowrap" data-toggle="modal" data-target="#scoresModal<%=validator.account%>">
  <i class="fas fa-circle mr-1 score-<%= validator.root_distance_score.to_i %>"
     title="Root Distance Score = <%= validator.root_distance_score %>"></i>

  <i class="fas fa-circle mr-1 score-<%= validator.vote_distance_score.to_i %>"
     title="Vote Distance Score = <%= validator.vote_distance_score %>"></i>

  <i class="fas fa-circle mr-1 score-<%= validator.skipped_slot_score.to_i %>"
     title="Skipped Slot Score = <%= validator.skipped_slot_score %>"></i>

  <i class="fas fa-circle mr-1 score-<%= validator.published_information_score.to_i %>"
     title="Published Information Score = <%= validator.published_information_score.to_i %>"></i>

  <i class="fas fa-circle mr-1 score-<%= validator.software_version_score.to_i %>"
     title="Software Version Score = <%= validator.software_version_score %>"></i>

  <i class="fas fa-circle mr-1 score-<%= validator.security_report_score.to_i %>"
     title="Bonus Point = <%= validator.security_report_score.to_i %>"></i>

  <% if validator.consensus_mods_score.to_i < 0 %>
    <i class="fas fa-minus-circle mr-1 text-warning"
       title="Consensus Mods Score = <%= validator.consensus_mods_score %>. Validator appears to use software modifications with an unproven effect on consensus."></i>
  <% end %>

  <% if validator.authorized_withdrawer_score.to_i < 0 %>
    <i class="fas fa-minus-circle mr-1 text-warning"
       title="Authorized Withdrawer Score = <%= validator.authorized_withdrawer_score.to_i %>"></i>
  <% end %>

  <% if validator.stake_concentration_score.to_i < 0 %>
    <i class="fas fa-minus-circle mr-1 text-warning"
       title="Stake Concentration Score = <%= validator.stake_concentration_score %>"></i>
  <% end %>

  <% if validator.data_center_concentration_score.to_i < 0 %>
    <i class="fas fa-minus-circle mr-1 text-warning"
       title="Data Center Concentration Score = <%= validator.data_center_concentration_score %>"></i>
  <% end %>

  (<span class="d-inline-block d-lg-none">Total:&nbsp;</span><%= validator.score.displayed_total_score %>)
</a>

<div class="modal modal-large fade" id="scoresModal<%=validator.account%>" tabindex="-1" role="dialog" aria-hidden="true">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h6 class="modal-title">
          Total score: <%= validator.score.displayed_total_score %>
          <span class="text-muted">(max 11)</span>
        </h6>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>

      <div class="modal-body p-0">
        <table class="table table-scores mb-0">
          <tbody>
          <tr>
            <td class="column-md font-weight-bold">Root Distance Score</td>
            <td class="column-xs text-nowrap">
              <i class="fas fa-circle mr-1 score-<%= validator.root_distance_score %>"></i>
              <%= validator.root_distance_score %>
              <small class="d-lg-none text-muted">(2)</small>
              <small class="d-none d-lg-inline text-muted">(max 2)</small>
            </td>
            <td class="column-md small">
              This score measures the&nbsp;median &&nbsp;average distance in block height between the&nbsp;validator and
              the&nbsp;tower's highest block.
            </td>
          </tr>
          <tr>
            <td class="column-md font-weight-bold">Vote Distance Score</td>
            <td class="column-xs text-nowrap">
              <i class="fas fa-circle mr-1 score-<%= validator.vote_distance_score.to_i %>"></i>
              <%= validator.vote_distance_score %>
              <small class="d-lg-none text-muted">(2)</small>
              <small class="d-none d-lg-inline text-muted">(max 2)</small>
            </td>
            <td class="column-lg small">
              This score is very similar to the&nbsp;Root Distance. Lower numbers mean that the&nbsp;node is voting near the front of the&nbsp;group.
            </td>
          </tr>
          <tr>
            <td class="column-md font-weight-bold">Skipped Slot Score</td>
            <td class="column-xs text-nowrap">
              <i class="fas fa-circle mr-1 score-<%= validator.skipped_slot_score.to_i %>"></i>
              <%= validator.skipped_slot_score %>
              <small class="d-lg-none text-muted">(2)</small>
              <small class="d-none d-lg-inline text-muted">(max 2)</small>
            </td>
            <td class="column-lg small">
              This score measures the&nbsp;percent of the&nbsp;time that a&nbsp;leader fails to produce a&nbsp;block during their allocated slots.
            </td>
          </tr>
          <tr>
            <td class="column-md font-weight-bold">Published Information Score</td>
            <td class="column-xs text-nowrap">
              <i class="fas fa-circle mr-1 score-<%= validator.published_information_score.to_i %>"></i>
              <%= validator.published_information_score %>
              <small class="d-lg-none text-muted">(2)</small>
              <small class="d-none d-lg-inline text-muted">(max 2)</small>
            </td>
            <td class="column-lg small">
              This score measures the&nbsp;contact information that the&nbsp;validator has posted to the&nbsp;blockchain.
            </td>
          </tr>
          <tr>
            <td class="column-md font-weight-bold">Software Version Score</td>
            <td class="column-xs text-nowrap">
              <i class="fas fa-circle mr-1 score-<%= validator.software_version_score.to_i %>"></i>
              <%= validator.software_version_score %>
              <small class="d-lg-none text-muted">(2)</small>
              <small class="d-none d-lg-inline text-muted">(max 2)</small>
            </td>
            <td class="column-lg small">
              This score is based on the&nbsp;Solana software version currently in use on this node.
            </td>
          </tr>
          <tr>
            <td class="column-md font-weight-bold">Bonus Point</td>
            <td class="column-xs text-nowrap">
              <i class="fas fa-circle mr-1 score-<%= validator.security_report_score.to_i %>"></i>
              <%= validator.security_report_score %>
              <small class="d-lg-none text-muted">(1)</small>
              <small class="d-none d-lg-inline text-muted">(max 1)</small>
            </td>
            <td class="column-lg small">
              Bonus Point is assigned for providing a&nbsp;URL to validator's web page related to security policies.
            </td>
          </tr>
          <tr>
            <td class="column-md font-weight-bold">Stake Concentration Score</td>
            <td class="column-xs text-nowrap">
              <% if validator.stake_concentration_score.to_i < 0 %>
                <i class="fas fa-minus-circle mr-1 text-warning"></i>
              <% end %>
              <%= validator.stake_concentration_score %>
              <small class="d-lg-none text-muted">(0)</small>
              <small class="d-none d-lg-inline text-muted">(max 0)</small>
            </td>
            <td class="column-lg small">
              This is a&nbsp;contra-score and will deduct points if the validator is one of the&nbsp;top 33%&nbsp;active stake holders.
            </td>
          </tr>
          <tr>
            <td class="column-md font-weight-bold">Data Center Concentration Score</td>
            <td class="column-xs text-nowrap">
              <% if validator.data_center_concentration_score.to_i < 0 %>
                <i class="fas fa-minus-circle mr-1 text-warning"></i>
              <% end %>
              <%= validator.data_center_concentration_score %>
              <small class="d-lg-none text-muted">(0)</small>
              <small class="d-none d-lg-inline text-muted">(max 0)</small>
            </td>
            <td class="column-lg small">
              This is a&nbsp;contra-score and will deduct points from the&nbsp;validator located in data centers that host a&nbsp;high percentage of other Solana validators.
            </td>
          </tr>
          <tr>
            <td class="column-md font-weight-bold">Authorized Withdrawer Score</td>
            <td class="column-xs text-nowrap">
              <% if validator.authorized_withdrawer_score.to_i < 0 %>
                <i class="fas fa-minus-circle mr-1 text-warning"></i>
              <% end %>
              <%= validator.authorized_withdrawer_score.to_i %>
              <small class="d-lg-none text-muted">(0)</small>
              <small class="d-none d-lg-inline text-muted">(max 0)</small>
            </td>
            <td class="column-lg small">
              This is a&nbsp;contra-score and will deduct points from the&nbsp;validator whose identity matches authorized withdrawer.
            </td>
          </tr>
          <tr>
            <td class="column-md font-weight-bold">Consensus Mods Score</td>
            <td class="column-xs text-nowrap">
              <% if validator.consensus_mods_score.to_i < 0 %>
                <i class="fas fa-minus-circle mr-1 text-warning"></i>
              <% end %>
              <%= validator.consensus_mods_score.to_i %>
              <small class="d-lg-none text-muted">(0)</small>
              <small class="d-none d-lg-inline text-muted">(max 0)</small>
            </td>
            <td class="column-lg small">
              This is a&nbsp;contra-score and will deduct points from the&nbsp;validator that appears to&nbsp;use unproven software modifications.
            </td>
          </tr>
          </tbody>
        </table>
        <div class="px-3 pb-3 pt-2 text-muted text-center">
          <small>See <%= link_to 'Our Scoring System', faq_path(anchor: 'score')%> page for more information.</small>
        </div>
      </div>

    </div>
  </div>
</div>
