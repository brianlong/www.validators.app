<canvas id="spark_line_skipped_vote_<%= local_assigns[:account] %>"></canvas>
<script type="text/javascript">
    var ctx = document.getElementById("spark_line_skipped_vote_<%= local_assigns[:account] %>").getContext('2d');
    var chart = new Chart(ctx, {
        type: 'gauge',
        data: {
            datasets: [{
                data: [-3, 0, 3, 6],
                value: <%= local_assigns[:value] %>,
                minValue: -6,
                maxValue: 6,
                backgroundColor: [
                    '<%= LIGHT_GREY_TRANSPARENT %>', '<%= GREEN_TRANSPARENT %>',
                    '<%= GREEN_TRANSPARENT %>', '<%= LIGHT_GREY_TRANSPARENT %>'
                ],
                borderColor: ['<%= LIGHT_GREY %>', '<%= GREEN %>', '<%= GREEN %>', '<%= LIGHT_GREY %>'],
                borderWidth: 0.5
            }]
        },
        options: {
            responsive: true,
            title: {
                display: false
            },
            layout: {
                padding: {
                    left: 0,
                    right: 0
                }
            },
            needle: {
                radiusPercentage: 0,
                widthPercentage: 5,
                lengthPercentage: 80,
                color: '<%= DARK_GREY %>'
            },
            valueLabel: {
                display: false
            }
        }
    });
</script>