development:
  adapter: redis
  url: <%= Rails.application.credentials.dig(:redis, :url) %>

test:
  adapter: test

stage:
  adapter: redis
  url: <%= ENV["REDIS_URL"] %>
  channel_prefix: validators_app_stage

production:
  adapter: redis
  url: <%= ENV["REDIS_URL"] %>
  channel_prefix: validators_app_production
